#include <stdio.h>

#define MAX 10
/*
 * input :12345
 * output :54321
 * len = 5
 * for (i = 0; i < len; i++)
 * 	d[len] = s[i];
 * 	len--
 */

int
getline2 (char line[])
{	

	return len;
}

void 
reverse(char destination[], char source[])
{
	int src_len = 0;
	int dest_len = 0;

	src_len = getline2(source);
	dest_len = src_len;
	dest_len = dest_len - 1;

	for (i = 0; i < src_len; i++) {
		destination[len] = source[i];
		dest_len = dest_len - 1;
	}

	for (i = 0; i < src_len; i++)
		printf("The reverse line is : %s\n", destination[i]);

}
int
main ()
{
	char curr_line[MAX] = {0};
	char reverse_line[MAX] = {0};
	int curr_len = 0;

	while ((c = getline))

	return 0;
}
